node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
# NEVER commit private keys
secrets/
*.pem
!scripts/*.example
.DS_Store

# Local-only build plans (contain commercial/control-plane strategy — keep out of the public repo)
docs/plan/
00-START-HERE.md
MANIFEST.txt

# local Claude Code settings
dist-types/

# a checkout's own data directory (see SENTRELLO_DATA_DIR). Both spellings:
# a relative path is resolved against the process's working directory, and the
# API server runs from apps/server while tests run from the root.
/data/
apps/server/data/
